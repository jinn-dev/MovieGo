<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mvg.mappers.wishlistMapper">
<insert id="insertWishlist" parameterType="Wishlist">
  	insert into wishlists (movie_code, user_id) values (#{movieCode}, #{userId})
	</insert>
	
	<select id="getWishlistByUserId" resultType="Wishlist">
	select m.movie_code, m.movie_title_kr, m.movie_directior, 
	m.movie_genre, w.user_id, w.movie_code, w.wish_id from movies m join wishlists w on(m.movieCode = w.movieCode) where user_id= #{userId}
	</select>
</mapper>

